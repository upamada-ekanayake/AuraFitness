# AuraFitness Datasets

Real datasets used for AuraFitness AI work belong here.

- `raw/` stores original downloaded datasets.
- `processed/` stores cleaned datasets created from scripts or notebooks.

Rules:

- No real dataset = no training.
- No dataset source = no dataset usage.
- No license check = no production usage.
