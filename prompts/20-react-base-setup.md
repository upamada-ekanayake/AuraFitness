# Prompt 20: React + TypeScript + Tailwind Base Setup

Goal:
Initialize a clean, professional React + TypeScript + Tailwind CSS (v4) development foundation inside the `app/` directory, importing and linking the exported AI types, and establishing responsive routing layouts with placeholders.

Required elements:
- Vite-based React + TypeScript scaffolding
- Tailwind CSS v4 integration
- Clean app folder structure
- AI export types linked in `src/types/ai.ts`
- Responsive navigation layout using React Router v6 and Lucide icons

Expected result:
A build-verified base React setup running locally at http://localhost:5173/ with working placeholders for all target views.
